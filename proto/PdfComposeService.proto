syntax = "proto3";
option go_package = ".;pdfcompose";

package pdfcompose;

service PdfCompose {
  rpc Send(stream Images) returns(stream Pdf);
}

message Images {
  oneof img {
    bool new_file = 1;
    bytes chunk_file = 2;
  }
}

message Pdf {
  bytes chunk_file = 1;
}