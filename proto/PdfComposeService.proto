syntax = "proto3";
option go_package = ".;pdfcompose";

package pdfcompose;

service PdfCompose {
  rpc Send(stream Images) returns(stream Pdf);
}

message Images {
  repeated bytes files = 1;
}

message Pdf {
    bytes result = 1;
}