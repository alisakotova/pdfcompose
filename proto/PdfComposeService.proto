syntax = "proto3";
option go_package = ".;pdfcompose";

package pdfcompose;

service PdfCompose {
  rpc Send(stream Images) returns(stream Pdf);
}

message Images {
  bytes chunk_upfile1 = 1;
  bytes chunk_upfile2 = 2;
  bytes chunk_upfile3 = 3;
}

message Pdf {
  bytes chunk_file = 1;
}